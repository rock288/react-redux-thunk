export interface User {
  admin: boolean;
  createdAt: string;
  displayName: string;
  email: string;
  firstName: string;
  lastName: string;
  refreshTokens: string;
  token: string;
  _id: string;
}
